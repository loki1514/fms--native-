<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.contactautopilot">

    <!--
    Place this file at: android/app/src/main/AndroidManifest.xml
    This manifest includes all required permissions for a SaaS contact management app
    -->

    <!-- ============================================ -->
    <!-- INTERNET & NETWORK PERMISSIONS -->
    <!-- ============================================ -->
    
    <!-- Required for network operations -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Required for network state checking -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Required for WiFi state checking -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- ============================================ -->
    <!-- CONTACTS PERMISSIONS (CRITICAL for SaaS) -->
    <!-- ============================================ -->
    
    <!-- Read contacts - Required for contact sync -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Write contacts - Required for contact management -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    
    <!-- ============================================ -->
    <!-- STORAGE PERMISSIONS -->
    <!-- ============================================ -->
    
    <!-- Read external storage - For importing contacts from files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- Write external storage - For exporting contacts -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    
    <!-- Media permissions for Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- ============================================ -->
    <!-- CAMERA PERMISSIONS -->
    <!-- ============================================ -->
    
    <!-- Camera access - For business card scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Camera feature (not required - app works without camera) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    
    <!-- ============================================ -->
    <!-- LOCATION PERMISSIONS (if using location features) -->
    <!-- ============================================ -->
    
    <!-- Approximate location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Precise location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Background location (Android 10+) - if needed for meeting logging -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- ============================================ -->
    <!-- MICROPHONE PERMISSIONS (if using voice notes) -->
    <!-- ============================================ -->
    
    <!-- Record audio - For voice notes -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- ============================================ -->
    <!-- NOTIFICATION PERMISSIONS -->
    <!-- ============================================ -->
    
    <!-- Post notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Foreground service - For background sync -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Foreground service data sync type (Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- ============================================ -->
    <!-- PHONE PERMISSIONS (if using call features) -->
    <!-- ============================================ -->
    
    <!-- Read phone state - For identifying incoming calls -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- Call phone - For initiating calls from app -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <!-- ============================================ -->
    <!-- CALENDAR PERMISSIONS (if integrating with calendar) -->
    <!-- ============================================ -->
    
    <!-- Read calendar - For meeting integration -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    
    <!-- Write calendar - For scheduling meetings -->
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    
    <!-- ============================================ -->
    <!-- BIOMETRIC PERMISSIONS (for secure authentication) -->
    <!-- ============================================ -->
    
    <!-- Use biometric hardware -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    
    <!-- Use fingerprint (legacy) -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- ============================================ -->
    <!-- BLUETOOTH PERMISSIONS (if using nearby sharing) -->
    <!-- ============================================ -->
    
    <!-- Bluetooth - Android 11 and below -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    
    <!-- Bluetooth Admin - Android 11 and below -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    
    <!-- Bluetooth Scan - Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    
    <!-- Bluetooth Connect - Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />
    
    <!-- Bluetooth Advertise - Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="s" />
    
    <!-- ============================================ -->
    <!-- SYSTEM PERMISSIONS -->
    <!-- ============================================ -->
    
    <!-- Receive boot completed - For scheduling background sync -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Vibrate - For notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Wake lock - For keeping device awake during sync -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Request install packages - For app updates -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    
    <!-- Query all packages - For checking if apps are installed -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- ============================================ -->
    <!-- APPLICATION CONFIGURATION -->
    <!-- ============================================ -->
    
    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="false"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:screenOrientation="portrait">
            
            <!-- Main launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep linking intent filter -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="contactautopilot" />
                <data android:scheme="https"
                    android:host="contactautopilot.com"
                    android:pathPrefix="/app" />
                <data android:scheme="https"
                    android:host="www.contactautopilot.com"
                    android:pathPrefix="/app" />
            </intent-filter>
            
            <!-- Intent filter for importing contacts -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="text/csv" />
                <data android:mimeType="text/comma-separated-values" />
                <data android:mimeType="text/vcard" />
                <data android:mimeType="text/x-vcard" />
            </intent-filter>
            
            <!-- Intent filter for vCard files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.vcf" />
                <data android:pathPattern=".*\\.vcard" />
            </intent-filter>
        </activity>

        <!-- Firebase Messaging Service -->
        <service
            android:name=".fcm.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Firebase Instance ID Service -->
        <service
            android:name=".fcm.InstanceIdService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <!-- Background Sync Service -->
        <service
            android:name=".sync.SyncService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <!-- Work Manager Initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- Boot Receiver for scheduling sync -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Network State Receiver -->
        <receiver
            android:name=".receivers.NetworkStateReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- Firebase Messaging Default Notification Channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- Firebase Messaging Default Notification Icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- Firebase Messaging Default Notification Color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />

        <!-- Google Play Services Version -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Disable Firebase Analytics collection (enable if needed) -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" />

        <!-- Disable Firebase Crashlytics collection (enable if needed) -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="true" />

        <!-- File Provider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

    <!-- Queries for package visibility (Android 11+) -->
    <queries>
        <!-- Query for browsers -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        
        <!-- Query for email apps -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        
        <!-- Query for phone apps -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        
        <!-- Query for SMS apps -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        
        <!-- Query for camera apps -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        
        <!-- Query for specific packages -->
        <package android:name="com.whatsapp" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.linkedin.android" />
        <package android:name="com.twitter.android" />
        <package android:name="com.google.android.gm" />
        <package android:name="com.microsoft.office.outlook" />
        <package android:name="com.salesforce.chatter" />
    </queries>

</manifest>
